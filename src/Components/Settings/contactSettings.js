import { View, Text } from 'react-native';

const settings = {
  viewContact: false,
  blocked: false,
};

export default function ContactSettings() {
  return (
    <View>
      <Text> Contact Settings drawer </Text>
    </View>
  );
}
